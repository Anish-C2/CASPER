/* CASPER final desktop pass: card-first home, one-page #segments, trophy-only clubs, compact player honours */
(function(){
  const esc=v=>escapeHtml(String(v==null?'':v));
  const cfgs=()=>STATE.sportsCfg.sports||[];
  const sp=id=>STATE.sports[id]||{matches:[],tournaments:[],teams:{},players:{},ranked:[]};
  const playerLink=p=>`<a href="#player/${encodeURIComponent(p.name)}">${esc(p.name)}</a>`;
  const teamLink=t=>`<a href="#team/${encodeURIComponent(t.abbr)}">${esc(t.name)}</a>`;
  const score=m=>m.kind==='cricket'?`${m.sh}/${m.hw} – ${m.sa}/${m.aw}`:`${m.sh}–${m.sa}`;
  const clubFor=p=>(p.clubNames&&p.clubNames.size)?[...p.clubNames].join(', '):'—';

  function css(){if(document.getElementById('casper-final-style'))return;const s=document.createElement('style');s.id='casper-final-style';s.textContent=`
    .cf-home{max-width:1480px;margin:0 auto}.cf-hero{display:grid;grid-template-columns:1.5fr 1fr;gap:8px;margin-bottom:8px}.cf-hero-main,.cf-card,.cf-seg{background:#fffdf6;border:1px solid #b9a77d}.cf-hero-main{padding:16px;background:linear-gradient(135deg,#142443,#22385b);color:#fff}.cf-eyebrow{font:700 10px Consolas;letter-spacing:1.2px;color:#e7d59d}.cf-hero h2{font:700 32px Georgia;margin:5px 0}.cf-hero p{font:12px Georgia;line-height:1.5;max-width:700px}.cf-hero-links{display:flex;gap:5px;flex-wrap:wrap;margin-top:12px}.cf-hero-links a,.cf-chip{display:inline-block;padding:6px 9px;border:1px solid #b9a77d;background:#fffdf6;color:#142443;font:700 10px Consolas;text-decoration:none}.cf-hero-links a:hover,.cf-chip:hover{background:#f1dfb7}.cf-hero-side{display:grid;grid-template-columns:1fr 1fr;gap:7px}.cf-card{padding:9px}.cf-card h3{margin:0 0 6px;font:700 12px Consolas;color:#142443}.cf-big{font:700 25px Georgia;color:#142443}.cf-small{font:9px Consolas;color:#6b6256;text-transform:uppercase}.cf-stat-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-bottom:9px}.cf-stat{padding:9px;background:#f1dfb7;border:1px solid #b9a77d;text-align:center}.cf-stat b{display:block;font:700 22px Georgia;color:#142443}.cf-stat span{font:8px Consolas;color:#6b6256;text-transform:uppercase}.cf-title{display:flex;align-items:center;gap:8px;margin:12px 0 7px;color:#142443;font:700 12px Consolas;letter-spacing:.5px}.cf-title i{height:1px;background:#b9a77d;flex:1}.cf-grid3{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.cf-grid2{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px}.cf-card .row{display:flex;justify-content:space-between;gap:7px;padding:6px 0;border-bottom:1px solid #e1d5bb;font:10px Consolas}.cf-card .row:last-child{border-bottom:0}.cf-card .row b{color:#142443}.cf-card .muted{font:10px Georgia}.cf-result{display:grid;grid-template-columns:72px 1fr auto;align-items:center;gap:7px;padding:7px 0;border-bottom:1px solid #e1d5bb}.cf-result:last-child{border-bottom:0}.cf-tag{font:8px Consolas;text-transform:uppercase;color:#6b6256}.cf-match{font:10px Georgia;color:#142443}.cf-score{font:700 14px Georgia;color:#142443;white-space:nowrap}.cf-comp{display:flex;align-items:center;justify-content:space-between;gap:7px;padding:7px;border-bottom:1px solid #e1d5bb;font:10px Consolas}.cf-comp:last-child{border-bottom:0}.cf-rank{display:flex;align-items:center;gap:7px;padding:7px 0;border-bottom:1px solid #e1d5bb}.cf-rank:last-child{border-bottom:0}.cf-rank-no{width:22px;text-align:center;font:700 10px Consolas;color:#8c6f27}.cf-rank-name{flex:1;font:10px Georgia}.cf-rank-score{font:700 10px Consolas;color:#142443}.cf-segments{display:block}.cf-seg{padding:9px;margin-top:8px;scroll-margin-top:10px}.cf-seg h3{margin:0 0 7px;font:700 13px Consolas;color:#142443}.cf-seg-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.cf-seg .cf-card{margin:0}.cf-search{display:flex;gap:5px;align-items:center}.cf-search input{flex:1;min-width:0}.cf-note{padding:8px;background:#f7f1e3;border:1px solid #d4c4a0;font:10px Georgia;color:#5f574b}.cf-club-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px}.cf-club{padding:8px;border:1px solid #d4c4a0;background:#fffdf6}.cf-club b{display:block;font:700 11px Georgia;color:#142443}.cf-club span{font:9px Consolas;color:#6b6256}.cf-awards{display:flex;gap:5px;flex-wrap:wrap}.cf-award{padding:6px 8px;background:#f1dfb7;border:1px solid #b9a77d;font:700 10px Consolas;color:#142443}.cf-trophy-list{display:flex;gap:6px;flex-wrap:wrap}.cf-trophy{padding:7px 9px;border:1px solid #b9a77d;background:#f7f1e3;font:700 10px Consolas;color:#142443}.cf-player-awards{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:5px}.cf-player-award{padding:7px;border:1px solid #d4c4a0;background:#fbf7ec;display:flex;justify-content:space-between;gap:6px;font:10px Consolas}.cf-player-award b{color:#142443}.cf-player-award span{color:#8c6f27;font-weight:700}.cf-full{margin-top:7px;text-align:right}.cf-full a{font:700 9px Consolas;color:#8c6f27}
    @media(max-width:1000px){.cf-hero,.cf-grid2{grid-template-columns:1fr}.cf-grid3,.cf-seg-grid{grid-template-columns:1fr 1fr}.cf-club-grid{grid-template-columns:repeat(2,1fr)}}
    @media(max-width:650px){.cf-stat-grid,.cf-grid3,.cf-seg-grid,.cf-club-grid{grid-template-columns:1fr}.cf-result{grid-template-columns:60px 1fr}.cf-score{grid-column:2}}
  `;document.head.appendChild(s)}

  function latest(n=8){const a=[];cfgs().forEach(c=>sp(c.id).matches.forEach(m=>a.push({c,m})));return a.slice(-n).reverse()}
  function leaders(id,field,n=5){return Object.values(sp(id).players||{}).sort((a,b)=>(b[field]||0)-(a[field]||0)||a.name.localeCompare(b.name)).slice(0,n)}
  function allChampions(){const out={};cfgs().forEach(c=>sp(c.id).tournaments.forEach(t=>{if(t.meta.e==='Seasonal Awards')return;const ch=t.aw&&t.aw.ch;if(!ch)return;out[ch]=(out[ch]||0)+1}));return out}
  function trophyCounts(abbr){let n=0;const list=[];cfgs().forEach(c=>sp(c.id).tournaments.forEach(t=>{if(t.meta.e==='Seasonal Awards'||!t.aw||t.aw.ch!==abbr)return;n++;list.push({name:t.meta.e||t.meta.id,sport:c.name,season:t.meta.s||''})}));return {n,list}}
  function aggregateAwards(p){const map={};(p.awards||[]).forEach(a=>{const label=String(a.label||a.name||'Award').trim();if(!label)return;map[label]=(map[label]||0)+1});return Object.entries(map).sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0]))}

  function homeStats(){const f=archiveFacts();return `<div class="cf-stat-grid"><div class="cf-stat"><b>${f.matches}</b><span>Matches</span></div><div class="cf-stat"><b>${f.players}</b><span>Players</span></div><div class="cf-stat"><b>${f.clubs}</b><span>Clubs</span></div><div class="cf-stat"><b>${f.tours}</b><span>Competitions</span></div></div>`}
  function sportCards(){return `<div class="cf-grid3">${cfgs().map(c=>{const s=sp(c.id),ps=leaders(c.id,'goals',3),unit=c.scoring==='cricket'?'Runs':'Goals',ch=Object.entries(allChampions()).sort((a,b)=>b[1]-a[1]).find(x=>s.teams[x[0]]);return `<div class="cf-card"><h3>${esc(c.name)}</h3><div class="cf-big">${s.matches.length}</div><div class="cf-small">matches · ${s.tournaments.length} competitions</div><div class="row"><span>Leader</span><b>${ps[0]?playerLink(ps[0]):'—'}</b></div><div class="row"><span>${unit}</span><b>${ps[0]?(ps[0].goals||0):0}</b></div><div class="row"><span>Latest</span><b>${s.tournaments.filter(t=>t.meta.e!=='Seasonal Awards').slice(-1)[0]?esc(s.tournaments.filter(t=>t.meta.e!=='Seasonal Awards').slice(-1)[0].meta.e):'—'}</b></div><div class="row"><span>Top club</span><b>${ch?s.teams[ch[0]]?teamLink(s.teams[ch[0]]):esc(ch[0]):'—'}</b></div></div>`}).join('')}</div>`}
  function leaderCards(){return `<div class="cf-grid3">${cfgs().map(c=>{const unit=c.scoring==='cricket'?'Runs':'Goals';const ls=leaders(c.id,'goals',5);return `<div class="cf-card"><h3>${esc(c.name)} · ${unit}</h3>${ls.map((p,i)=>`<div class="cf-rank"><span class="cf-rank-no">${i+1}</span><span class="cf-rank-name">${playerLink(p)}<br><small>${esc(clubFor(p))}</small></span><b class="cf-rank-score">${p.goals||0}</b></div>`).join('')}<div class="cf-full"><a href="#top-scorers">Full ${esc(c.name)} leaders →</a></div></div>`}).join('')}</div>`}
  function resultsCards(){return `<div class="cf-grid2">${cfgs().map(c=>{const rows=latest(12).filter(x=>x.c.id===c.id).slice(0,6);return `<div class="cf-card"><h3>Latest ${esc(c.name)} results</h3>${rows.map(x=>{const m=x.m,hn=m.names&&m.names[m.home]?m.names[m.home].name:m.home,an=m.names&&m.names[m.away]?m.names[m.away].name:m.away;return `<div class="cf-result"><span class="cf-tag">${esc(m.stageLabel||m.stage||'Result')}</span><span class="cf-match">${esc(hn)}<br><b>vs</b> ${esc(an)}</span><span class="cf-score">${score(m)}</span></div>`}).join('')||'<div class="cf-note">No results in the current archive.</div>'}<div class="cf-full"><a href="#results">All results →</a></div></div>`}).join('')}</div>`}
  function compCards(){const rows=allTourneys().filter(x=>x.t.meta.e!=='Seasonal Awards').slice().reverse().slice(0,12);return `<div class="cf-grid2"><div class="cf-card"><h3>Competition archive</h3>${rows.map(x=>`<div class="cf-comp"><span>${esc(x.sport.name)}</span><a href="#competition/${encodeURIComponent(x.t.meta.id)}">${esc(x.t.meta.e||x.t.meta.id)}</a><span>${esc(x.t.meta.s||'')}</span></div>`).join('')}<div class="cf-full"><a href="#archive">Open archive →</a></div></div><div class="cf-card"><h3>Global club ranking</h3>${globalRanks().slice(0,10).map((r,i)=>`<div class="cf-rank"><span class="cf-rank-no">${i+1}</span><span class="cf-rank-name">${teamLink({abbr:r.abbr,name:r.name})}</span><b class="cf-rank-score">${r.avgRank.toFixed(2)}</b></div>`).join('')}<div class="cf-full"><a href="#ranking">Full ranking →</a></div></div></div>`}
  function home(){css();return `<div class="cf-home"><div class="cf-hero"><div class="cf-hero-main"><div class="cf-eyebrow">COMPETITIVE ATHLETICS & SPORTS PROMOTION</div><h2>CASPER ARCHIVE</h2><p>The central record of CASPER competition history, clubs, players, results, rankings and honours — presented as an archive, not a spreadsheet.</p><div class="cf-hero-links"><a href="#archive">BROWSE ARCHIVE</a><a href="#players">PLAYER ARCHIVE</a><a href="#teams">CLUB ARCHIVE</a><a href="#statistics">STATISTICS</a></div></div><div class="cf-hero-side"><div class="cf-card"><h3>ARCHIVE STATUS</h3><div class="cf-big">ONLINE</div><div class="cf-small">CSN data source</div></div><div class="cf-card"><h3>SPORTS</h3><div class="cf-big">${cfgs().length}</div><div class="cf-small">active archive desks</div></div><div class="cf-card"><h3>LATEST MATCHES</h3><div class="cf-big">${latest(1).length?latest(1)[0].m.event||'—':'—'}</div><div class="cf-small">most recent record</div></div><div class="cf-card"><h3>TOP CLUB</h3><div class="cf-big">${globalRanks()[0]?esc(globalRanks()[0].name):'—'}</div><div class="cf-small">global archive rank</div></div></div></div>${homeStats()}<div class="cf-title"><span>SPORT DESKS</span><i></i></div>${sportCards()}<div class="cf-title"><span>PLAYER LEADERS</span><i></i></div>${leaderCards()}<div class="cf-title"><span>RECENT RESULTS</span><i></i></div>${resultsCards()}<div class="cf-title"><span>ARCHIVE SNAPSHOT</span><i></i></div>${compCards()}<div class="cf-segments">${segments()}</div></div>`}

  function seg(id,title,body){return `<section class="cf-seg" id="${id}"><h3>${title}</h3>${body}</section>`}
  function segmentResults(){return `<div class="cf-grid3">${latest(15).map(x=>{const m=x.m,hn=m.names&&m.names[m.home]?m.names[m.home].name:m.home,an=m.names&&m.names[m.away]?m.names[m.away].name:m.away;return `<div class="cf-card"><h3>${esc(x.c.name)}</h3><div class="cf-small">${esc(m.event||'')} · ${esc(m.stageLabel||m.stage||'')}</div><p><b>${esc(hn)}</b> vs <b>${esc(an)}</b></p><div class="cf-big">${score(m)}</div></div>`}).join('')||'<div class="cf-note">No results.</div>'}</div>`}
  function segmentStandings(){return `<div class="cf-grid3">${cfgs().map(c=>`<div class="cf-card"><h3>${esc(c.name)} table</h3>${(sp(c.id).ranked||[]).slice(0,10).map((t,i)=>`<div class="cf-rank"><span class="cf-rank-no">${i+1}</span><span class="cf-rank-name">${teamLink(t)}<br><small>${t.wins}-${t.draws}-${t.losses}</small></span><b class="cf-rank-score">${t.pts}</b></div>`).join('')}</div>`).join('')}</div>`}
  function segmentGoals(field,title){return `<div class="cf-grid3">${cfgs().map(c=>`<div class="cf-card"><h3>${esc(c.name)} · ${title}</h3>${leaders(c.id,field,10).map((p,i)=>`<div class="cf-rank"><span class="cf-rank-no">${i+1}</span><span class="cf-rank-name">${playerLink(p)}<br><small>${esc(clubFor(p))}</small></span><b class="cf-rank-score">${p[field]||0}</b></div>`).join('')||'<div class="cf-note">No records.</div>'}</div>`).join('')}</div>`}
  function segmentClean(){return `<div class="cf-grid3">${cfgs().map(c=>{const list=Object.values(sp(c.id).teams||{}).sort((a,b)=>(b.cleanSheets||0)-(a.cleanSheets||0)).slice(0,10);return `<div class="cf-card"><h3>${esc(c.name)} · Clean sheets</h3>${list.map((t,i)=>`<div class="cf-rank"><span class="cf-rank-no">${i+1}</span><span class="cf-rank-name">${teamLink(t)}</span><b class="cf-rank-score">${t.cleanSheets||0}</b></div>`).join('')||'<div class="cf-note">No clean-sheet data.</div>'}</div>`}).join('')}</div>`}
  function segmentPlayers(){const list=Object.values(collectPlayers()).sort((a,b)=>a.name.localeCompare(b.name));return `<div class="cf-card"><div class="cf-search">${searchBox()}</div><div class="cf-club-grid" style="margin-top:8px">${list.slice(0,32).map(p=>`<div class="cf-club"><b>${playerLink(p)}</b><span>${esc(clubFor(p))}</span></div>`).join('')}</div><div class="cf-full"><a href="#players">Open full player archive →</a></div></div>`}
  function segmentCompare(){return `<div class="cf-note">Player comparison is available from the archive. Select two registered players to compare their sport records.</div><div class="cf-hero-links" style="margin-top:7px"><a href="#compare-players">OPEN PLAYER COMPARISON →</a></div>`}
  function segmentClubs(){const map=collectTeams().sort((a,b)=>(b.titles||0)-(a.titles||0)||a.name.localeCompare(b.name));return `<div class="cf-club-grid">${map.slice(0,40).map(t=>{const tc=trophyCounts(t.abbr);return `<div class="cf-club"><b>${teamLink(t)}</b><span>${tc.n} ${tc.n===1?'trophy':'trophies'} · ${t.matches||0} matches</span></div>`}).join('')}</div>`}
  function segmentAwards(){const rows={};allTourneys().forEach(({t})=>Object.keys(t.aw||{}).forEach(code=>{const label=labelAward(code);rows[label]=(rows[label]||0)+1}));return `<div class="cf-awards">${Object.entries(rows).sort((a,b)=>b[1]-a[1]).map(x=>`<div class="cf-award">${esc(x[0])} ×${x[1]}</div>`).join('')||'<div class="cf-note">No awards.</div>'}</div>`}
  function segmentRecords(){const f=archiveFacts();return `<div class="cf-grid3">${[['Matches',f.matches],['Players',f.players],['Clubs',f.clubs],['Competitions',f.tours],['Goals',f.goals],['Runs',f.runs],['Clean sheets',f.cs],['Hat-tricks',f.ht],['Awards',f.awards]].map(x=>`<div class="cf-card"><div class="cf-big">${x[1]}</div><div class="cf-small">${x[0]}</div></div>`).join('')}</div>`}
  function segmentStats(){return `<div class="cf-grid3">${cfgs().map(c=>{const s=sp(c.id);const goals=Object.values(s.players||{}).reduce((n,p)=>n+(p.goals||0),0);return `<div class="cf-card"><h3>${esc(c.name)}</h3><div class="row"><span>Matches</span><b>${s.matches.length}</b></div><div class="row"><span>Competitions</span><b>${s.tournaments.length}</b></div><div class="row"><span>Clubs</span><b>${Object.keys(s.teams||{}).length}</b></div><div class="row"><span>${c.scoring==='cricket'?'Runs':'Goals'}</span><b>${goals}</b></div></div>`}).join('')}</div>`}
  function segmentNews(){return `<div class="cf-card">${generateNews().slice(0,12).map(n=>`<div class="cf-comp"><span>${esc(n)}</span><span>→</span></div>`).join('')||'<div class="cf-note">No news.</div>'}</div>`}
  function segmentGeneric(title,text,links){return `<div class="cf-note">${text}</div><div class="cf-hero-links" style="margin-top:7px">${(links||[]).map(x=>`<a href="${x[0]}">${esc(x[1])}</a>`).join('')}</div>`}
  function segments(){return [
    seg('live-scores','MATCH CENTRE · LIVE SCORES',segmentResults()),
    seg('fixtures','MATCH CENTRE · FIXTURES',segmentGeneric('Fixtures','The current CSN archive is result-led. When future fixtures are serialized, they will appear here automatically.')),
    seg('results','MATCH CENTRE · RESULTS',segmentResults()),
    seg('league-tables','MATCH CENTRE · LEAGUE TABLES',segmentStandings()),
    seg('top-scorers','PLAYER CENTRE · GOALS BY SPORT',segmentGoals('goals','Goals / Runs')),
    seg('top-assists','PLAYER CENTRE · ASSISTS BY SPORT',segmentGoals('assists','Assists')),
    seg('clean-sheets','PLAYER CENTRE · CLEAN SHEETS',segmentClean()),
    seg('player-search','PLAYER CENTRE · PLAYER SEARCH',segmentPlayers()),
    seg('compare-players','PLAYER CENTRE · COMPARE PLAYERS',segmentCompare()),
    seg('club-directory','ARCHIVE · CLUB DIRECTORY',segmentClubs()),
    seg('awards','ARCHIVE · AWARDS',segmentAwards()),
    seg('ranking','ARCHIVE · GLOBAL RANKING',`<div class="cf-grid2"><div class="cf-card">${globalRanks().slice(0,15).map((r,i)=>`<div class="cf-rank"><span class="cf-rank-no">${i+1}</span><span class="cf-rank-name">${teamLink({abbr:r.abbr,name:r.name})}</span><b class="cf-rank-score">${r.avgRank.toFixed(2)}</b></div>`).join('')}</div><div class="cf-note">Global ranking remains club-based. Player honours never enter the club trophy tally.</div></div>`),
    seg('records','ARCHIVE · RECORDS',segmentRecords()),
    seg('statistics','ARCHIVE · STATISTICS',segmentStats()),
    seg('news','CASPER · NEWS',segmentNews()),
    seg('about','CASPER · ABOUT',segmentGeneric('About','CASPER is the Competitive Athletics & Sports Promotion archive. This hub preserves serialized competition records across football, futsal and cricket.')),
    seg('downloads','CASPER · DOWNLOADS',segmentGeneric('Downloads','Archive exports and serialized records are kept in the repository.','')),
    seg('rules','CASPER · RULES & FORMATS',segmentGeneric('Rules & formats','CSN is the source format used to serialize competition records, matches, awards and rankings.')),
    seg('site-map','CASPER · SITE MAP',segmentGeneric('Site map','Everything on the main CASPER hub is reachable through the navigation above or the archive segments.'))
  ].join('')}

  /* Club aggregation: championships only. Individual awards/honours are deliberately excluded. */
  const originalCollectTeams=window.collectTeams;
  window.collectTeams=function(){const base=originalCollectTeams?originalCollectTeams():[];return base.map(t=>{const tc=trophyCounts(t.abbr);return Object.assign({},t,{titles:tc.n,trophies:tc.list.map(x=>({label:'Champion',event:x.name,season:x.season,sport:x.sport})),honours:[]})})};

  /* Compact player honours: Champion ×4, Golden Boot ×5, etc. */
  const originalPlayer=window.renderPlayer;
  if(originalPlayer){window.renderPlayer=function(name){const html=originalPlayer(name);const holder=document.createElement('div');holder.innerHTML=html;const p=collectPlayers().find(x=>x.name.toLowerCase()===decodeURIComponent(name).toLowerCase());const box=holder.querySelector('.player-awards');if(box&&p){const awards=aggregateAwards(p);box.innerHTML='<h3>Awards & honours</h3>'+(awards.length?'<div class="cf-player-awards">'+awards.map(x=>'<div class="cf-player-award"><b>'+esc(x[0])+'</b><span>×'+x[1]+'</span></div>').join('')+'</div>':'<p class="muted">No honours recorded.</p>')}return holder.innerHTML}};

  /* Home is card-first; no tables. */
  window.renderHubHome=home;

  const oldRoute=window.route;
  const segmentIds=new Set(['live-scores','fixtures','results','league-tables','top-scorers','top-assists','clean-sheets','player-search','compare-players','club-directory','awards','ranking','records','statistics','news','about','downloads','rules','site-map']);
  window.route=function(){const raw=(location.hash||'#home').slice(1),id=raw.split('/')[0];if(segmentIds.has(id)&&PAGE.mode==='hub'){document.getElementById('app').innerHTML=home();setActive(id==='club-directory'?'teams':id==='top-scorers'||id==='top-assists'||id==='clean-sheets'||id==='player-search'||id==='compare-players'?'players':id==='awards'||id==='ranking'||id==='records'||id==='statistics'?'archive':id==='news'||id==='about'||id==='downloads'||id==='rules'||id==='site-map'?'about':'home');setTimeout(()=>document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'}),0);return}return oldRoute()};

  function install(){if(window.STATE&&STATE.sportsCfg&&STATE.sportsCfg.sports&&STATE.sportsCfg.sports.length){css();window.route()}else setTimeout(install,100)}install();
})();
