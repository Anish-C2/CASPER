/* CASPER player compatibility stub.
   Player rendering is owned by casper-desktop-site.js.
   This file must not override the archive router or call renderPlayer recursively. */
(function(){
  'use strict';
})();
